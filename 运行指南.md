# AegisDesk AI 运行指南

## 1. 环境要求

- Python 3.11+
- Node.js 18+
- MySQL 8+
- Qdrant Cloud 或本地 Qdrant
- 可用的 LLM API Key
- 可用的 Embedding API Key
- 可选：百炼/通义 Rerank API Key

## 2. 初始化数据库

演示环境可以开启启动初始化，让 FastAPI 启动时自动创建数据库、创建表，并导入 `testdocs` 下的测试文档：

```env
BOOTSTRAP_ON_STARTUP=true
```

也可以在项目根目录手动执行初始化函数：

```powershell
python testdocs\bootstrap.py
```

如果不使用自动初始化，也可以手动创建 MySQL 数据库并执行初始化脚本。

创建 MySQL 数据库并执行初始化脚本：

```powershell
cd backend
mysql -uroot -p < 数据库初始化脚本/init.sql
```

## 3. 后端启动

```powershell
cd backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
```

编辑 `.env`，至少配置：

```text
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER_NAME=root
MYSQL_USER_PASSWORD=你的密码
MYSQL_DB_NAME=aegisdeskai
BOOTSTRAP_ON_STARTUP=true

QDRANT_URL=https://xxxx.qdrant.tech
QDRANT_API_KEY=你的 Qdrant Key
QDRANT_COLLECTION=knowledge_chunks

LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=你的 LLM Key
LLM_MODEL=qwen3.6-flash

EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=你的 Embedding Key
EMBEDDING_MODEL=text-embedding-v4

RERANK_URL=https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank
RERANK_API_KEY=你的百炼 Key
RERANK_MODEL=gte-rerank-v2
```

启动后端：

```powershell
uvicorn app.main:app --reload --port 8000
```

健康检查：

```text
http://127.0.0.1:8000/health
```

期望返回：

```json
{"status":"ok","service":"AegisDesk AI"}
```

## 4. 前端启动

```powershell
cd frontend
npm install
npm run dev
```

如果 npm 下载慢，可以使用国内镜像：

```powershell
npm install --registry=https://registry.npmmirror.com
```

访问：

```text
http://localhost:5173
```

## 5. 常用配置说明

| 配置项                       | 说明                                  |
| ------------------------- | ----------------------------------- |
| `BOOTSTRAP_ON_STARTUP`    | 是否启动时自动创建数据库并导入 `testdocs`，默认 false |
| `DAILY_QUESTION_LIMIT`    | 每个用户每日提问次数上限，默认 100，设置为 0 表示不限制     |
| `MAX_QUESTION_LENGTH`     | 单次提问最大长度，默认 500                     |
| `RAG_TOP_K`               | 最终给 LLM 使用的知识片段数量                   |
| `RAG_REWRITE_QUERY_COUNT` | 问题改写 query 数                        |
| `RAG_RECALL_PER_QUERY`    | 每个 query 召回数量                       |
| `RAG_RRF_TOP_K`           | RRF 融合后保留候选数量                       |
| `RAG_SCORE_THRESHOLD`     | Rerank 相关性阈值                        |
| `RAG_DEBUG_LOG_ENABLED`   | 是否记录 RAG 检索、RRF、Rerank 调试日志         |
| `RAG_DEBUG_LOG_TOP_N`     | 每个 RAG 阶段最多打印的 chunk 数量             |

## 6. 测试建议

1. 注册并登录用户。
2. 上传企业知识库文档，如 FAQ、退换货政策。
   - FAQ/QA 文档建议使用 `Q：/A：`、`问：/答：`、`问题：/答案：` 格式；系统识别出 3 组及以上问答时，会按“一问一答一个 chunk”切分。
   - 点击上传后，页面会先显示“处理中”，后端解析、向量化完成后状态会更新为“就绪”或“失败”。
   - 如果重复上传同样内容，页面会弹窗提示“已存在相同内容的文档”，并移除本地临时上传行；旧数据没有 `file_hash` 时，会按同名同类型文档做兜底提示。
   - 知识库是企业共享的，任意登录用户上传后，其他登录用户也可以查看和检索。
3. 提问知识库相关问题，观察回答末尾是否按文档聚合展示知识来源和 chunk 摘要。
4. 提问时观察聊天气泡是否显示“正在识别问题类型、正在检索知识库、正在生成回答”等阶段进度，并在模型输出后替换为正式回答。
5. 提问知识库无关问题，观察是否走兜底回答。
6. 点赞或踩 AI 回答，刷新后确认状态保持。
7. 将 `DAILY_QUESTION_LIMIT` 临时设为 2，测试次数限制。

## 7. 日志位置

后端日志文件：

```text
backend/logs/app.log
```

LLM、Embedding、Qdrant、Rerank 相关异常会记录在这里，前端只展示用户友好的提示。

开启 `RAG_DEBUG_LOG_ENABLED=true` 后，RAG 每个阶段的候选 chunk、文档名、分数、Rerank 分数和摘要也会写入该文件，便于排查无关文档为什么进入最终回答上下文。
