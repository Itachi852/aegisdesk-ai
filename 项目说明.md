# 项目说明

本项目是一个 AI 智能客服系统，目标是实现从用户提问、意图识别、知识检索、Prompt 拼接、LLM 生成到流式返回的完整 RAG 对话链路。

## 技术选型

- 前端选择 React + TypeScript，便于实现聊天界面、状态管理和后台页面。
- 后端选择 FastAPI，适合异步接口、SSE 流式输出和 AI 服务集成。
- 数据库选择 MySQL，用于保存用户、会话、消息、反馈和知识库元数据。
- 向量数据库选择 Qdrant，支持持久化、过滤检索和较好的工程化部署。
- Embedding 选择 bge-m3，中文效果较好，可本地运行。
- LLM 默认使用通义千问 API，也可替换为 OpenAI 兼容接口。

## AI 工程问题

检索为空时，系统返回标准兜底话术，不让模型自由编造。

上下文过长时，只携带最近 N 轮对话，并对更早历史做摘要。

为降低幻觉，Prompt 明确要求只能基于知识库回答，同时记录引用来源，方便追溯。

当检索结果很多时，系统会按相似度、文档优先级和规则类型排序，优先保留高价值规则，避免注意力稀释。

## AI 编程工具使用说明

开发过程中可使用 AI 编程助手生成基础代码，但 RAG 的切分策略、检索阈值、Prompt 约束、异常处理和流式接口需要人工理解、调整和验证。
